language: csharp
sudo: required
dist: trusty
os:
  - linux
  - osx
addons:
  apt:
    packages:
    - gettext
    - libcurl4-openssl-dev
    - libicu-dev
    - libssl-dev
    - libunwind8
    - zlib1g
mono:
  - 4.2.1
script:
  - ./condo.sh